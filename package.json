{
  "license": "UNLICENSED",
  "main": "main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron ./main.js",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .ts",
    "pack:win32": "npm install && npm run build && npx electron-packager ./ BoRevenge --platform=win32 --arch=x64 --out=dist",
    "pack:macOS": "npm install && npm run build && npx electron-packager ./ BoRevenge --platform=darwin --arch=x64 --out=dist"
  },
  "devDependencies": {
    "@types/node": "^18.7.15",
    "@typescript-eslint/eslint-plugin": "^5.36.2",
    "@typescript-eslint/parser": "^5.36.2",
    "electron": "^20.1.4",
    "electron-packager": "^16.0.0",
    "eslint": "^8.23.0",
    "ts-node": "^10.9.1",
    "typescript": "4.8.2"
  }
}
